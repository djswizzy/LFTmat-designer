// Shared tool types so Toolbar and the page can stay in sync without
// dragging in the larger page module.

export type Tool =
  | "pencil"
  | "eraser"
  | "bucket"
  | "text"
  | "rectangle"
  | "ellipse"
  | "line"
  | "image";
